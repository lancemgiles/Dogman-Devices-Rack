#include "plugin.hpp"
#include "Grammar.hpp"

// this project is a bit too ambitious for right now, but the panel is ready
// following bogaudio's pgmr, it looks like this calls on the addr, meaning I may as well make a basic sequencer
// and then use that as a building block for something more complex